import styled from "@emotion/styled";
import React from "react";
import { RouteComponentProps } from "@reach/router";

const Styled = {};

/**
 * Scrollable pane that handles data fetching and loading
 * for a list of current actionable insights at /dashboard/current
 */
function CurrentInsights(_: RouteComponentProps) {
  // TODO implement
  return null;
}

export default CurrentInsights;
